/*
 * NotificationToast - Floating notification toasts
 * Appears in the top-right corner when new notifications arrive
 */

export function NotificationToast() {
  // Notifications are handled via the TopHUD dropdown for now
  // This component is a placeholder for future animated toast notifications
  return null;
}
